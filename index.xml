<craft>
<parameter name="size" type="int" default="2"/>
<script type="text/openjscad">
    //Main function
   function main() 
   {
   return moon({'size':10});
   }

 //Moon
 function moon(params)
 {
  var size=params.size||0.1;
  if(size<0.1)
  size=0.1
  var m1= cylinder({r: 2, h: 0.2});
  var m2= cylinder({r:2,h:0.2}).translate([0,1,0]);

  return difference(m1,m2);
 }
   </script>
</craft>
